[project]
name = "generate-middleware"
authors = ["Web3 Developer"]
description = "A decentralized middleware platform for secure service interaction tracking"
telemetry = false
requirements = []
[contracts.middleware-registry]
path = "contracts/middleware-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
